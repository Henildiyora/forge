# Optional. The swarm runs offline with the deterministic Ops planner when unset.
ANTHROPIC_API_KEY=

# Optional. Required only for `swarm run --commit-source live`.
GITHUB_TOKEN=

# Defaults match docker-compose.yml.
PROMETHEUS_URL=http://localhost:9090
