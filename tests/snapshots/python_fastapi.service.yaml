apiVersion: v1
kind: Service
metadata:
  name: sample-fastapi
  namespace: default
spec:
  selector:
    app: sample-fastapi
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP
